tj-init(1) -- Initialize the virtual machine
====================================

## SYNOPSIS

`tj init` [--flag=<arg>] [--flag]

## DESCRIPTION

Initializes the virtual machine.

## GLOBAL OPTIONS

  * `--vm-provision`, `--provision-vm`, `--provision`:
    Run provisioners after initializing the virtual machine
